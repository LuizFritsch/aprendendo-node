/*const app = require("../app_antigo");
//it("Should do something", () => {});
//test("Deve fazer algo", () => {});

describe("operacoes basicas", () => {
    test("Deve retornar o valor 10 ao somar 5 + 5", () => {
        let resultado = app.soma(5, 5);
        //expect(resultado).toEqual(10);
        //to be tem q ser do mesmo tipo
        expect(resultado).toBe(10);
    });

    test("Deve retornar o valor 10 ao multiplicar 2 e 5", () => {
        let resultado = app.multiplica(2, 5);
        expect(resultado).toBe(10);
    });
});*/